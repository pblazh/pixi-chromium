define([], function(){
    'use strict';

    return {
        GAME_WIDTH: 400,
        GAME_HEIGHT: 600,

        KEY_LEFT: 'KEY_LEFT',
        KEY_RIGHT: 'KEY_RIGHT',
        KEY_FIRE: 'KEY_FIRE',
        KEY_BREAK: 'KEY_BREAK',
        KEY_ACCELERATE: 'KEY_ACCELERATE',
    };
});
